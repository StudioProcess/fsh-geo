{
  "name": "three-starter",
  "version": "0.0.1",
  "description": "",
  "repository": "github:StudioProcess/three_starter",
  "engines": {
    "node": ">=8.9.0",
    "npm": ">=5.6.0"
  },
  "scripts": {
    "start": "browser-sync start --server --port 8080 --no-notify --files index.html app",
    "test": "echo \"Error: no test specified\" && exit 1",
    "reinstall": "rm -rf node_modules && npm install",
    "bundle": "rollup --config && node update_hash"
  },
  "keywords": [],
  "author": "Process <contact@process.studio> (https://process.studio)",
  "license": "AGPL-3.0",
  "dependencies": {
    "dat.gui": "^0.7.1",
    "three": "^0.90.0"
  },
  "devDependencies": {
    "browser-sync": "^2.23.5",
    "rollup": "^0.62.0",
    "rollup-plugin-multi-entry": "^2.0.2",
    "rollup-plugin-terser": "^1.0.1"
  }
}
