{
  "name": "fsh-geo",
  "version": "0.0.1",
  "description": "",
  "repository": "github:StudioProcess/fsh-geo",
  "engines": {
    "node": ">=8.9.0",
    "npm": ">=5.6.0"
  },
  "scripts": {
    "start": "browser-sync start --server --port 8080 --no-notify --files index.html app",
    "test": "echo \"Error: no test specified\" && exit 1",
    "reinstall": "rm -rf node_modules && npm ci",
    "bundle": "rollup --config && node update_hash",
    "build": "npm run bundle"
  },
  "keywords": [],
  "author": "Process <contact@process.studio> (https://process.studio)",
  "license": "AGPL-3.0",
  "dependencies": {
    "dat.gui": "^0.7.5",
    "three": "^0.101.1"
  },
  "devDependencies": {
    "browser-sync": "^2.23.5",
    "rollup": "^1.4.1",
    "rollup-plugin-buble": "^0.19.6",
    "rollup-plugin-multi-entry": "^2.1.0",
    "rollup-plugin-terser": "^4.0.4"
  }
}
